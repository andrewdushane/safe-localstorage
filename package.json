{
  "name": "safe-localstorage",
  "version": "1.0.0",
  "description": "localStorage wrapper that fails gracefully when it's not available, like in Safari private mode",
  "main": "index.js",
  "scripts": {
    "babel": "babel src.js -o index.js",
    "test": "jest --verbose"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-babili": "^0.1.4",
    "babel-preset-env": "^1.6.0",
    "jest": "^20.0.4"
  },
  "jest": {
    "globals": {
      "window": {}
    }
  }
}
